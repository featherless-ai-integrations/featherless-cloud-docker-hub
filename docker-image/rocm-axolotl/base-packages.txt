# Supplied by the digest-pinned ROCm base, never resolved from PyPI.
torch
torchvision
torchaudio
triton
pytorch-triton-rocm
# Installed separately from the reviewed native wheel lock.
flash-attn
# Built separately from the optimizer source lock against that exact base.
deepspeed
